[project]
name = "scans_to_books"
version = "0.1.0"
description = ""
authors = [
    {name = "lexskiies",email = "alexis.cheyy@gmail.com"}
]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.9"
PyQt6 = "^6.9.0"
gallery-dl = "^1.30"

[tool.poetry.scripts]
scans-to-books = "scans_to_books.main:main"

[tool.poetry]
packages = [{include = "scans_to_books", from = "src"}]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
